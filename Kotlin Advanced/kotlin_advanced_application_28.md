## 요구사항 분석
워너비슈퍼추어소프트에서 근무하는 슈퍼추어는 한글날을 맞이하여 한국어로 쓰인 어떤 글에서, 조사를 비롯한 문법적 요소로 쓰이는 ```은```, ```는```, ```을```, ```를```의 등장에 따라 점수를 매기려고 한다. 그 규칙은 다음과 같다.
* 문법적 요소로 쓰이는 ```은``` 또는 ```는```은 다음의 일부이다. (단, ~은 공백을 제외한 어떤 것을 의미한다)
  * ```은데```, ```~은지```, ```~은 ``` (공백), ```는지```, ```는데```, ```는다``` ```는 ``` (공백)
* 문법적 요소로 쓰이는 ```을``` 또는 ```를```은 다음의 일부이다.
  * ```을지```, ```을까```, ```을 ``` (공백), ```를지```, ```를까```, ```를 ``` (공백)
* 처음에는 ```11,172점```으로 시작하며, 글은 앞부분부터 차례대로 읽어 나간다.
* 문법적 요소로 쓰이는 ```은```, ```는```, ```을```, ```를```을 만나면 다음과 같이 한다.
  * ```은```이 등장하면 1점, ```는```이 등장하면 5점을 더한다.
    * 단, ```을```, ```를```을 사이에 두지 않고 ```는```이 n번 등장하면 (단, n은 3 이상), 이후에 ```을```, ```를```이 등장하거나 글이 끝나기 직전의 마지막 ```는```에서 ```5 * n```점을 추가로 더한다.
    * 단, ```을```, ```를```을 사이에 두지 않고 ```은```과 ```는```이 합쳐서 n번 등장하면 (단, n은 10 이상), 이후에 ```을```, ```를```이 등장하거나 글이 끝나기 직전의 마지막 ```은``` 또는 ```는```에서 ```2 * n```점을 추가로 더한다.
  * ```을```이 등장하면 1점, ```를```이 등장하면 5점을 뺀다.
    * 단, ```는```을 사이에 두지 않고 ```를```이 n번 등장하면 (단, n은 3 이상), 이후에 ```는```이 등장하거나 글이 끝나기 직전의 마지막 ```를```에서 ```7 * n```점을 추가로 뺀다.
    * 단, ```는```을 사이에 두지 않고 ```을```과 ```를```이 합쳐서 n번 등장하면 (단, n은 5 이상), 이후에 ```는```이 등장하거나 글이 끝나기 직전의 마지막 ```을``` 또는 ```를```에서 ```3 * n```점을 추가로 뺀다.

어떤 글을 읽어 나갈 때, 문법적 요소로 쓰이는 ```은```, ```는```, ```을```, ```를```을 만날 때마다 현재 만난 해당 글자와 점수 변동(추가로 더하거나 빼는 점수의 각 세부 사항 포함)을 출력하는 프로그램을 작성하시오.

### 데이터 클래스
데이터 클래스 없이, 글을 나타내는 문자열이 주어진다.

## 코드 및 실행 결과
### 코드
```kotlin
// 점수 계산 및 출력
fun computeAndPrintScore(document: String) {
    var score = 11172
    
    var neun = 0 // "는" 3번 이상 등장
    var eunNeun = 0 // "은","는" 10번 이상 등장
    var reul = 0 // "를" 3번 이상 등장
    var eulReul = 0 // "을","를" 5번 이상 등장
    
    // 문법적 요소로 쓰이는 것만 추출
    val grammatical = document.filterIndexed { idx, it ->
        when (it) {
            '은' -> {
                document.length >= idx + 2 && ( 
                    document.substring(idx, idx + 2) == "은데" ||
                    (document.substring(idx, idx + 2) == "은지" && idx >= 1 && document[idx - 1] != ' ') ||
                    (document.substring(idx, idx + 2) == "은 " && idx >= 1 && document[idx - 1] != ' ')
                )
            }
            '는' -> {
                document.length >= idx + 2 && 
                document.substring(idx, idx + 2) in listOf("는지", "는데", "는다", "는 ")
            }
            '을' -> {
                document.length >= idx + 2 && 
                document.substring(idx, idx + 2) in listOf("을지", "을까", "을 ")
            }
            '를' -> {
                document.length >= idx + 2 && 
                document.substring(idx, idx + 2) in listOf("를지", "를까", "를 ")
            }
            else -> {
                false
            }
        }
    }
    println("1. 문법적 요소 : ${grammatical}\n")
    
    // 점수 계산하기
    grammatical.forEachIndexed { idx, it ->
        val addedScore = when (it) {
            '은' -> {
                eunNeun++
                1
            }
            '는' -> {
                eunNeun++
                neun++
                eulReul = 0
                reul = 0
                5
            }
            '을' -> {
                eulReul++
                eunNeun = 0
                neun = 0
                -1
            }
            '를' -> {
                eulReul++
                reul++
                eunNeun = 0
                neun = 0
                -5
            }
            else -> { 0 }
        }
        
        // '는' 연속 점수, '은', '는' 연속 점수 계산
        var neunBonus = 0
        var eunNeunBonus = 0
        
        if (idx == grammatical.length - 1 || grammatical[idx + 1] in listOf('을', '를')) {
            if (neun >= 3) {
                neunBonus = 5 * neun
            }
            if (eunNeun >= 10) {
                eunNeunBonus = 2 * eunNeun
            }
        }
        
        // '를' 연속 점수, '을', '를' 연속 점수 계산
        var reulBonus = 0
        var eulReulBonus = 0
        
        if (idx == grammatical.length - 1 || grammatical[idx + 1] == '는') {
            if (reul >= 3) {
                reulBonus = -7 * reul
            }
            if (eulReul >= 5) {
                eulReulBonus = -3 * eulReul
            }
        }
        
        // 점수 출력
        score += addedScore + neunBonus + eunNeunBonus + reulBonus + eulReulBonus
        println("글자: ${it}, 현재 점수: ${score}, 등장에 따른 점수 변동: ${addedScore}")
        println("보너스 점수: ${neunBonus}(는), ${eunNeunBonus}(은,는), ${reulBonus}(를), ${eulReulBonus}(을,를)\n")
    }
}

fun main() {
    val document = (
        "먼저, 나는 위대하다고 백만 번은 말할 수 있는 한글을 창제하신 세종대왕님께 깊은 감사를 드린다.\n" +
        "워너비슈퍼추어소프트는 소프트웨어 개발 회사이다.\n" +
        "이 회사에는 100명이 근무하고 있으며, 그 중에는 은지라는 이름을 가진 여직원이 있다.\n" +
        "그녀는 자신의 은지라는 이름을 자랑스럽게 생각한다.\n" +
        "이 회사의 서버 개발을 맡은 어떤 개발자를 은 목걸이로 감동시킨 어떤 고객을 회사의 이름을 걸고 극진히 대하라는 말이 있는데,\n" +
        "이 말은 지나가는 직원들은 물론 외부에서 오는 고객들까지 이 회사의 서비스 정신은 위대하다고 생각하게 한다.\n" +
        "두서가 없는 글이지만, 이 글은 코딩 테스트의 예제로는 매우 적합한 글이다. 이 글은 분명히 그런 글일 것이다.\n" +
        "이 말을 하는 자는 개발자이고, 그렇지 않는 자는 개발자가 아니다.\n" +
        "이 회사의 서버 팀은 30명의 직원으로 구성되어 있으며, 서버를 개발하고, DB를 개발하고, 웹뷰를 개발한다.\n" +
        "이 팀을 인수하기를 원한다면 당신은 이미 그 계획부터가 잘못되었다고 말할 정도로, 이 팀은 위대하다.\n" +
        "클라이언트 팀은 20명으로 구성되어 있으며, 그 팀에서 만든 앱은 기능이 없는 것이 없을 정도로 다양하다.\n" +
        "나머지는 기획 팀과 회계 팀인데, 기획 팀에서 디자인을 할까? 그런 사람이 없을까? 없을지도 모른다.\n" +
        "회계 팀에는 10명이 속해 있다. 이 팀은 회계를 한다. 이 팀은 회계를 한다. 이 팀은 분명히 회계를 한다.\n" +
        "그런데 회계가 과연 잘 되는지는 아무도 모른다. 회계 팀 리더는 알까?"
    )
    
    computeAndPrintScore(document)
}
```

### 실행 결과
```kotlin
1. 문법적 요소 : 는은는을은를는는는는을는는을을은를을을는는은는은는은는은는은을는는는는은를를를을를은은은은는을는을을을는은를은를은를는는는

글자: 는, 현재 점수: 11177, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11178, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11183, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11182, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11183, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11178, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11183, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11188, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11193, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11218, 등장에 따른 점수 변동: 5
보너스 점수: 20(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11217, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11222, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11227, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11226, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11225, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11226, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11221, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11220, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11204, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), -15(을,를)

글자: 는, 현재 점수: 11209, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11214, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11215, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11220, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11221, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11226, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11227, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11232, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11233, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11238, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11291, 등장에 따른 점수 변동: 1
보너스 점수: 30(는), 22(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11290, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11295, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11300, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11305, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11310, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11331, 등장에 따른 점수 변동: 1
보너스 점수: 20(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11326, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11321, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11316, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11315, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11310, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11311, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11312, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11313, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11271, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), -28(를), -15(을,를)

글자: 는, 현재 점수: 11276, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11275, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11280, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11279, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11278, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 을, 현재 점수: 11277, 등장에 따른 점수 변동: -1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11282, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11283, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11278, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11279, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11274, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 은, 현재 점수: 11275, 등장에 따른 점수 변동: 1
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 를, 현재 점수: 11249, 등장에 따른 점수 변동: -5
보너스 점수: 0(는), 0(은,는), -21(를), 0(을,를)

글자: 는, 현재 점수: 11254, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11259, 등장에 따른 점수 변동: 5
보너스 점수: 0(는), 0(은,는), 0(를), 0(을,를)

글자: 는, 현재 점수: 11279, 등장에 따른 점수 변동: 5
보너스 점수: 15(는), 0(은,는), 0(를), 0(을,를)
```
